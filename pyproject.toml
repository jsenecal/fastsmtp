[project]
name = "fastsmtp-workspace"
version = "0.1.5"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "aiosqlite>=0.20",
    "bump-my-version>=1.5.1",
    "httpx>=0.28",
    "mypy>=1.19",
    "pre-commit>=4.5.1",
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "pytest-cov>=4.0",
    "respx>=0.21",
    "ruff>=0.4",
    "testcontainers[postgres]>=4.13",
]
docs = [
    "zensical>=0.0.56",
]

[tool.uv.workspace]
members = ["fastsmtp", "fastsmtp-cli"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "ASYNC"]
ignore = ["B008"]  # Allow function calls in default arguments (FastAPI Depends pattern)

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["fastsmtp/tests", "fastsmtp-cli/tests"]
filterwarnings = [
    "ignore::DeprecationWarning:aiosmtpd.*",
    "ignore::DeprecationWarning:testcontainers.*",
    "ignore::DeprecationWarning:spf.*",
    "ignore::SyntaxWarning:spf.*",
]

[tool.bumpversion]
current_version = "0.1.5"
commit = true
tag = true
tag_name = "v{new_version}"
tag_message = "v{new_version}"
message = "chore: bump version {current_version} -> {new_version}"

[[tool.bumpversion.files]]
filename = "fastsmtp/pyproject.toml"
search = "^version = \"{current_version}\""
replace = "version = \"{new_version}\""
regex = true

[[tool.bumpversion.files]]
filename = "fastsmtp-cli/pyproject.toml"
search = "^version = \"{current_version}\""
replace = "version = \"{new_version}\""
regex = true

[[tool.bumpversion.files]]
filename = "fastsmtp/src/fastsmtp/__init__.py"
search = "__version__ = \"{current_version}\""
replace = "__version__ = \"{new_version}\""

[[tool.bumpversion.files]]
filename = "fastsmtp-cli/src/fastsmtp_cli/__init__.py"
search = "__version__ = \"{current_version}\""
replace = "__version__ = \"{new_version}\""
